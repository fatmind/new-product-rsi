// lib/consts.js —— 跨模块共享的实验常量（唯一出处，flow 和自迭代 prompt 都从这读）

// 破零线：新品发布后 7 日累计销量 ≥ 这个数 = 破零。
// 定成 5（不是 1）：错推的品蹭不过去（陷阱簇撑死 1-2 单）、擦线波动不会左右破零率，
// 真簇要"选对 + 补贴到位（匹配系数拉满）"才过线——激励是生死手，自迭代才有持续优化空间。
export const ZERO_BREAK_THRESHOLD = 5;
